from pathlib import Path

content = """# review.md

## 문서 목적

이 문서는 코드 리뷰, 리팩토링, 품질 점검 시 따라야 하는 검토 기준을 정의한다.  
이 문서는 구현 문서가 아니다.  
이 문서는 설계 분석 문서가 아니다.  
이 문서는 “이미 작성된 결과물이 프로젝트 규칙에 맞는가”를 검증한다.

이 문서의 목적은 다음과 같다.

- 구현 결과물이 공통 규칙을 지키는지 확인한다.
- 인터페이스 안정성, 정책 준수, 테스트 누락 여부를 점검한다.
- 중복 구현, 과도한 추상화, 숨은 side effect를 발견한다.
- 로깅, 에러 처리, LiteLLM 사용 원칙 위반 여부를 확인한다.
- 이후 다른 AI가 이어서 작업해도 안정성을 유지할 수 있게 코드 품질을 정리한다.

---

## 적용 범위

이 문서는 다음 상황에 적용한다.

- 신규 기능 구현 후 검토
- 기존 코드 수정 후 검토
- 리팩토링 후 검토
- 배포 전 품질 점검
- PR 전 자체 검토
- 공통 모듈 수정 후 회귀 점검
- Tool / Provider / API 변경 후 검토

이 문서는 다음 내용을 직접 다루지 않는다.

- 신규 설계 절차
- 구현 시작 전 분석 절차
- 언어 선택 기준
- 특정 프레임워크의 세부 사용법
- 기능 요구사항 자체의 타당성 분석

---

## 핵심 원칙

### 1. 동작 안정성을 먼저 본다
코드가 예쁘게 보이는지보다 기존 동작과 계약을 안전하게 유지하는지를 먼저 본다.

### 2. 계약 위반을 가장 먼저 찾는다
요청/응답 구조, 상태값, 에러 구조, Tool 계약, 환경변수 계약이 깨졌는지 먼저 확인한다.

### 3. 정책 위반을 숨기지 않는다
LiteLLM 우회, 민감정보 로그, 테스트 누락, 숨은 side effect는 즉시 드러내고 수정 대상으로 본다.

### 4. 중복과 과잉 추상화를 동시에 경계한다
같은 로직의 복제도 문제다.  
근거 없는 추상화 추가도 문제다.

### 5. 리뷰는 설명이 아니라 판단이다
좋아 보인다는 표현보다, 지켜졌는지/깨졌는지/수정이 필요한지를 분명히 판단한다.

### 6. 리팩토링은 목적이 명확해야 한다
리팩토링 결과가 구조 안정성, 가독성, 테스트 가능성 중 무엇을 개선했는지 설명 가능해야 한다.

---

## 반드시 먼저 확인할 검토 항목

### 인터페이스
- 요청 구조가 기존 계약과 호환되는가
- 응답 구조가 기존 계약과 호환되는가
- 상태값 사용이 표준과 일치하는가
- 에러 응답 구조가 일관적인가

### LiteLLM / LLM 경로
- LLM 호출이 LiteLLM Gateway를 우선하는가
- 모델 식별자가 alias 기준으로 유지되는가
- 직접 Provider SDK 호출이 무분별하게 추가되지 않았는가

### 안정성
- 예외 처리 누락이 없는가
- 실패 경로가 최소한으로라도 고려되었는가
- 숨은 side effect가 없는가
- 읽기 전용과 상태 변경 처리가 분리되어 있는가

### 테스트
- 핵심 수정에 대응하는 테스트가 존재하는가
- 테스트가 실제 변경 의도를 검증하는가
- mock / offline 경로가 깨지지 않았는가

### 운영성
- 로그 구조가 유지되는가
- 민감정보가 로그에 노출되지 않는가
- 감사 이력 또는 추적 가능한 정보가 남는가
- 설정 또는 환경변수 변경이 문서에 반영되었는가

---

## 리뷰 체크리스트

### 1. 계약 호환성 체크
- 기존 요청/응답 필드가 불필요하게 바뀌지 않았는가
- 상태값이 임의로 추가/변경되지 않았는가
- 외부 호출자에 영향을 주는 breaking change가 숨어 있지 않은가
- Tool 입력/출력 계약이 예고 없이 바뀌지 않았는가

### 2. 코드 구조 체크
- 기존 구조와 책임 분리가 유지되는가
- 새 파일/새 레이어 추가가 정당한가
- 공통 모듈이 있는데 중복 구현이 생기지 않았는가
- 한 함수/클래스가 여러 책임을 무리하게 떠안지 않았는가

### 3. LiteLLM 체크
- LiteLLM Gateway 경유 원칙이 유지되는가
- Provider 직접 연동이 있다면 예외 사유가 명확한가
- 인증 정보, URL, 모델명이 하드코딩되지 않았는가

### 4. 에러 처리 체크
- 예외를 조용히 삼키지 않는가
- 실패 시 최소한의 로그나 오류 반환이 있는가
- 정상 경로만 가정한 구현이 아닌가
- 외부 호출 실패 시 무한 재시도, 무응답, 무로깅 상태가 아닌가

### 5. 로깅 체크
- 로그가 너무 없지 않은가
- 로그가 너무 많아 노이즈만 만들지 않는가
- 민감정보가 로그에 남지 않는가
- request_id, session_id, agent_name 등 추적 키를 유지하는가

### 6. 테스트 체크
- 기능 수정에 맞는 테스트가 추가 또는 수정되었는가
- 테스트가 의미 있는 assert를 하는가
- mock / offline 경로를 깨지 않았는가
- 테스트 없이 핵심 로직이 변경되지 않았는가

### 7. 리팩토링 체크
- 리팩토링 범위가 목적에 비해 과도하지 않은가
- 리팩토링으로 가독성 또는 유지보수성이 실제로 좋아졌는가
- 단순 취향 변경을 대규모 수정으로 포장하지 않았는가

---

## 안정성 점검 기준

아래 중 하나라도 해당하면 위험 신호로 본다.

- side effect가 함수 이름과 구조에서 드러나지 않음
- 외부 호출 실패 시 처리 경로가 없음
- 상태 변경과 조회가 섞여 있음
- 공통 계약을 암묵적으로 변경함
- 예외 로그 없이 실패함
- 테스트 없이 핵심 분기 변경
- 환경변수 누락 시 비정상 동작이 발생함
- Gateway 우회 호출이 조용히 들어감

### 반드시 확인할 질문
- 이 변경은 기존 호출자를 깨뜨릴 수 있는가
- 이 변경은 운영 중 장애를 더 찾기 어렵게 만드는가
- 이 변경은 실패 시 어디에서 알 수 있는가
- 이 변경은 나중에 다른 AI가 읽었을 때 쉽게 이해 가능한가

---

## 중복 제거 기준

### 중복으로 판단하는 경우
- 같은 의미의 로직이 다른 이름으로 반복된다
- 같은 검증 로직이 여러 파일에 복제된다
- 같은 Provider / Tool 래핑 로직이 여러 위치에서 다시 작성된다
- 같은 응답 변환 로직이 여러 핸들러에 중복된다

### 중복이 아닌 경우
- 책임이 다르다
- 계약이 다르다
- 향후 재사용보다 현재 독립성이 더 중요하다
- 공통화 비용이 실제 이득보다 크다

### 중복 제거 원칙
- 공통화는 실제 반복이 확인된 후에 한다
- 한 번 쓰이는 로직은 함부로 공통 모듈로 올리지 않는다
- 중복 제거가 오히려 탐색 비용을 늘리면 보류한다

---

## 로깅 / 에러 처리 점검 기준

### 좋은 상태
- 실패 지점이 로그나 에러 코드로 드러난다
- 추적 키가 유지된다
- 민감정보는 마스킹된다
- 외부 호출 실패가 조용히 무시되지 않는다

### 나쁜 상태
- except / catch 후 아무 처리 없음
- print 또는 임시 로그만 남음
- request context 없이 로그만 찍힘
- 운영에 필요한 오류 정보가 없음
- 비밀번호, 토큰, 키, 개인식별정보가 로그에 노출됨

---

## 정책 위반 여부 점검 기준

아래는 정책 위반 또는 고위험으로 본다.

- LiteLLM 우회 후 아무 설명이 없음
- 실제 키 또는 비밀값이 코드에 포함됨
- 테스트 없이 핵심 로직 수정
- 공통 인터페이스를 조용히 변경
- 문서 또는 설정 반영이 필요한 변경을 숨김
- side effect가 승인/검토 없이 확대됨
- Python과 TypeScript에 동일 로직을 근거 없이 중복 구현

---

## 인터페이스 호환성 점검 기준

### 반드시 확인한다
- 필드명 변경 여부
- 필드 타입 변경 여부
- 상태값 추가/변경 여부
- null 가능성 변화 여부
- Tool 스키마 변화 여부
- 환경변수 키 이름 변화 여부

### 호환성 위반으로 보는 경우
- 호출자 수정이 필요한데 문서화되지 않음
- 기존 테스트가 깨지는데 의도 설명이 없음
- 응답 구조가 달라졌는데 상태값만 그대로 유지됨
- 같은 엔드포인트에서 의미가 달라짐

---

## 테스트 누락 점검 기준

아래 중 하나라도 해당하면 테스트 누락 가능성이 높다.

- 핵심 로직 변경인데 테스트 파일 수정이 전혀 없음
- 분기 추가가 되었는데 새 케이스가 없음
- 에러 처리 추가가 되었는데 실패 케이스 테스트가 없음
- 외부 연동 방식이 바뀌었는데 integration 검증이 없음
- mock / offline 모드 관련 코드가 바뀌었는데 해당 테스트가 없음

### 최소 기준
- 정상 경로 1개 이상
- 핵심 실패 경로 검토
- 계약 변경 시 관련 테스트 갱신

---

## 금지사항

- “동작하니까 됨” 수준으로 리뷰 종료
- 구조 위반을 스타일 차이로 넘김
- LiteLLM 우회를 묵인
- 테스트 누락을 사소한 문제로 처리
- 인터페이스 변경을 문서화 없이 승인
- 민감정보 노출 가능성을 무시
- 중복 구현을 “나중에 정리”로 반복 허용
- 리뷰 범위를 넘어 신규 설계까지 섞어서 판단

---

## 예외 처리 기준

### 1. 긴급 핫픽스
긴급 수정은 허용할 수 있다.  
단, 아래는 반드시 확인한다.

- 직접 영향 계약
- 실패 시 로그
- 최소 테스트
- 후속 정리 필요 여부

### 2. 테스트 추가가 어려운 경우
아래를 남긴다.

- 왜 지금 테스트를 추가하지 못하는지
- 어떤 위험이 남는지
- 추후 어떤 테스트가 필요한지

### 3. LiteLLM 예외 경로가 필요한 경우
아래를 모두 만족해야 한다.

- 기술적으로 Gateway 경유가 현재 불가능하다
- 예외 사유가 문서 또는 코드에 남아 있다
- 추후 정리 방향이 분명하다

---

## 다른 문서와의 경계

### 이 문서가 담당하는 것
- 구현 결과물의 품질 점검 기준
- 안정성 점검 기준
- 중복 제거 기준
- 로깅 / 에러 처리 점검
- 정책 위반 여부 확인
- 인터페이스 호환성 확인
- 테스트 누락 점검

### 이 문서가 담당하지 않는 것
- 설계 절차
- 구현 시작 전 분석 절차
- 실제 코드 작성 규칙
- 언어 선택 기준
- Python / TypeScript 저수준 구현 방식

### 관련 문서 참조 기준
- 구현 전 분석은 `plan.md`
- 실제 구현 규칙은 `build.md`
- Python 상세 구현 기준은 `runtime-python.md`
- TypeScript 상세 구현 기준은 `runtime-typescript.md`
- 프로젝트 전체 공통 규칙은 `CLAUDE.md`

---

## 짧은 예시

### 좋은 리뷰 예시
- LiteLLM 경유 원칙 유지 확인
- 응답 구조 변경 여부 확인
- 테스트 추가 여부 확인
- side effect 함수 분리 여부 확인
- 민감정보 로그 노출 여부 확인

### 나쁜 리뷰 예시
- 코드 스타일만 보고 종료
- “잘 동작할 것 같음”으로 승인
- 테스트 누락을 무시
- 공통 계약 변경을 놓침
- 예외 처리 누락을 발견하지 못함

---

## 최종 행동 규칙

먼저 계약을 본다.  
그다음 안정성을 본다.  
그다음 LiteLLM, 로그, 에러 처리, 테스트를 본다.  
중복과 과잉 추상화를 함께 점검한다.  
문제가 있으면 명확히 지적한다.  
애매하면 승인하지 않는다.
"""

path = Path("/mnt/data/review.md")
path.write_text(content, encoding="utf-8")
print(f"Saved to {path}")
